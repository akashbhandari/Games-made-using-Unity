using UnityEngine;
using System.Collections;

public class doneInClass : MonoBehaviour {

	////////	New one
	/*
		bool isEqual = horizontalInput == verticalInput;
		bool isGreaterThan = horizontalInput > verticalInput;
		Debug.Log (isEqual);

		if (horizontalInput == 1) {
			Debug.Log ("Horizontal is 1!");
		} else if (horizontalInput == 0) {
			Debug.Log ("Horizontal is 0!");
		} else if (horizontalInput == -1) {
			Debug.Log ("Horizontal is -1!");	
		}
		Vector3 ballPosition = physicsBody.position;
		if (ballPosition.y<0){
			Debug.Log ("Goodbye!");
		}
*/	
	/*
	void OntriggerEnter(Collider otherCollider) {
		if (otherCollider.CompareTag ("Pickup")) {
			Debug.Log ("Ran into pickup");
			else if (otherCollider.CompareTag("EvilPickup")){
				Debug.Log ("Oh No");
			}
		}
	}

	//NOTE: use for loop than while

	void CountToTen() {
		int i;
		while (i<=10) {
			Debug.Log (i);
			i += 1;
		}
	}
	for (int j = 1; j <= 10; j++) {
		Debug.Log (j);
	}
}
*/




}
